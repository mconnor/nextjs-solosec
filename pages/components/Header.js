import styled from 'styled-components'

const MainDiv = styled.div`
    background: green;
`;

const Header = () => {
    return (
        <MainDiv>
        Header
        </MainDiv>
    )
}

export default Header
